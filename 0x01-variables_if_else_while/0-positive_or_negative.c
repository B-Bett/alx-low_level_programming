#include <stdlib.h>
#include <time.h>
/**
 * more header goes there
 * Return: 0(Success)
 */
int main(void)
{
int n;
strand(time(0));
n = rand() - RAND_MAX / 2;
/* the code goes there */
return (0);
}
